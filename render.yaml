services:
  - type: web
    name: consistency-daily
    runtime: node
    rootDir: ai-service
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 5002
      - key: NODE_ENV
        value: production
